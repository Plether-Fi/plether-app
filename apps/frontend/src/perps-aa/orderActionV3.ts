// The pinned SDK owns V3 validation, timing authority, and calldata encoding.
export {
  buildPlaceOrderV3Action,
  buildProtectedOpenAction as buildProtectedOpenV3Action,
} from '@plether-fi/perps-aa-client'
